\begin{align*}
  \operatorname{Cov}\left[z^{(m)}_{m}, z^{(m)}_{p}\right]
  &= \frac{\beta^{2} \rho_{y}}{4 V_{E} + 4 \beta^{2}} && \text{[}z^{(m)}_{m} \leftrightarrow z^{(m)}_{m} \rightarrow g_m \rightarrow y_m \;\text{---}\; y_p \leftarrow g_p \leftarrow z^{(m)}_{p} \leftrightarrow z^{(m)}_{p}\text{]} \\
  &= \frac{\beta^{2} \rho_{y}}{4 \left(V_{E} + \beta^{2}\right)}
\end{align*}